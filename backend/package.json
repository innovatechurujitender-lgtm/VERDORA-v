{
  "name": "verdora-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.cjs",
    "dev": "nodemon server.cjs",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "postinstall": "node -e \"const fs=require('fs'),p=require('path');const src=p.join(__dirname,'..','frontend','dist');const dest=p.join(__dirname,'public');if(fs.existsSync(src)){if(fs.existsSync(dest))fs.rmSync(dest,{recursive:true});fs.cpSync(src,dest,{recursive:true});console.log('Frontend build copied to public/');}\""
  },
  "dependencies": {
    "@netlify/functions": "^5.3.0",
    "@supabase/supabase-js": "^2.108.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "nodemailer": "^9.0.0",
    "qrcode": "^1.5.4",
    "resend": "^6.12.4",
    "serverless-http": "^4.0.0",
    "sib-api-v3-sdk": "^8.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
