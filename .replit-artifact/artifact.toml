kind = "web"
previewPath = "/"
title = "VERDORA Marketplace"
version = "1.0.0"
id = "artifacts/verdora"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24805

[services.development]
run = "pnpm --filter @workspace/verdora run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/verdora", "run", "build" ]
publicDir = "artifacts/verdora/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24805"
BASE_PATH = "/"
